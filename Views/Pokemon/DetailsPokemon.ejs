<%- include('../layout', { title: pokemon.nombre }) %>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            <img src="<%= pokemon.url_imagen %>" alt="<%= pokemon.nombre %>" class="img-fluid">
        </div>
        <div class="col-md-8">
            <h1><%= pokemon.nombre %></h1>
            <p><strong>Tipo:</strong> <%= pokemon.Tipo ? pokemon.Tipo.nombre : 'Desconocido' %></p>
            <p><strong>Región:</strong> <%= pokemon.Region ? pokemon.Region.nombre : 'Desconocida' %></p>
            <a href="/pokemones" class="btn edit-button back-button">Volver a la lista</a>
        </div>
    </div>
</div>